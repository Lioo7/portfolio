version: 1
frontend:
  phases:
    build:
      commands: []
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []

# Redirect rules for 404 handling
redirects:
  - source: '/<*>'
    target: '/404.html'
    status: '404'
    condition: null